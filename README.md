# Supera Challenges

The values ​​for executing each challenge are automatically set by methods of each class in order to gain delivery time. However, the classes referring to each challenge offer methods so that values ​​can be added dynamically using the 'Scanner'.